# Minimal deps for Streamlit Community Cloud + GitHub Actions (fetch/clean/update scripts).
# For a full local environment (spaCy, etc.): pip install -r requirements-full.txt
streamlit>=1.40,<2
pandas>=2.0,<3
requests>=2.28
python-dotenv>=1.0
